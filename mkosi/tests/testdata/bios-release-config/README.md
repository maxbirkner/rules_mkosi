# BIOS runtime fixture

This deterministic, offline release configuration adds only the serial-console
and one-shot shutdown settings needed to qualify the legacy BIOS image under
the pinned SeaBIOS toolchain.
