# Consumer BIOS runtime fixture

The independent consumer uses this offline release configuration to prove that
the public SeaBIOS boot-test API boots a real GRUB BIOS image and observes its
serial readiness and shutdown protocol.
